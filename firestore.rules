rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /entries/{entryId} {
      allow read, write: if request.auth != null
        && request.auth.token.email == 'tianbian.agi@gmail.com';
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
